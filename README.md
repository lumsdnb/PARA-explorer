# streamlined project planner

## frontend

- run `npm i && npm run watch'

## backend

- cd into `/backend` and run `npm i && npm run dev`

## desktop

- `/src-tauri` contains everything, run `npm run tauri dev` to start desktop dev server
